import { DateTransferPipe } from './date-transfer.pipe';

describe('DateTransferPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTransferPipe();
    expect(pipe).toBeTruthy();
  });
});
